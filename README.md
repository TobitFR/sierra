# SIERRA QTG Pipeline

Automated processing pipeline for QTG (Qualification Test Guide) printouts generated by the **SIERRA** flight simulator software running on a **Sun IPC** workstation. Captures print jobs, converts them to PDF via **RetroPrinter** on a Raspberry Pi, pairs text and graph files belonging to the same QTG test, and delivers a single merged PDF per test to a maintenance management computer (MMGT).

Currently in use for A330/A340 Full Flight Simulator qualification testing.

---

## Architecture Overview

```
┌─────────────────────────────────┐
│          Sun IPC (SunOS)        │
│                                 │
│  SIERRA ──► lpsierra (spooler)  │
│              dfA0NNs12sun       │
│                                 │
│  m.spts.sierra-deamon.sh        │
│  (polls every 60s)              │
│         │                       │
│         │ FTP (.raw × 2)        │
└─────────┼───────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────┐
│                 Raspberry Pi (RPi)                  │
│                                                     │
│  /home/pi/data/raw/   ◄── dfA0NNs12sun.raw  (OCR)   │
│  /home/pi/data/pdf/   ◄── dfA0NNs12sun.raw  (conv)  │
│                                                     │
│  RetroPrinter converts .raw ──► .pdf                │
│  then triggers CustomScript.sh                      │
│                                                     │
│  CustomScript.sh  (flock serialized)                │
│    │                                                │
│    ├── ocr_rename.sh                                │
│    │     Pairs PDF ↔ RAW by exact filename          │
│    │     Extracts QTG code + simulator prefix       │
│    │     Renames: [SIM] CODE date §NNN.pdf          │
│    │                                                │
│    ├── group_qtg.sh  (also runs via cron */2 min)   │
│    │     Pairs text + graph by consecutive §NNN     │
│    │     Merges with pdfunite (text first)          │
│    │     Releases solo files after 5min timeout     │
│    │                                                │
│    └── FTP transfer ──► MMGT (D:/QTG)               │
│          (skips files still carrying §NNN)          │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## How It Works

### 1. Print job capture (Sun IPC)

SIERRA sends print jobs to a fake printer (`lpsierra`) managed by a custom `printcap` entry. The daemon `m.spts.sierra-deamon.sh` polls the spooler directory every 60 seconds. Each `df*` job file (e.g. `dfA047s12sun`) is renamed to `.raw` and transferred via FTP to the RPi in two copies: one for RetroPrinter (conversion) and one for OCR identification.

The spooler sequence number embedded in the filename (`dfA0NN`, 000–999 with rollover) is the key that links a text file and its companion graph file from the same QTG test.

### 2. PDF conversion (RetroPrinter)

RetroPrinter on the RPi converts each `.raw` file to a `.pdf`, assigning its own filename in the format `retro-printer_YYYY-MM-DD_HHMMSS[-FULL].pdf` (the `-FULL` suffix indicates a multi-page document). The original `dfA0NNs12sun` filename is not preserved. On completion it triggers `CustomScript.sh`.

RetroPrinter processes files strictly one at a time.

### 3. Identification and renaming (`ocr_rename.sh`)

Called by `CustomScript.sh` with the full path of the just-converted PDF as argument (e.g. `/home/pi/data/pdf/retro-printer_2026-06-27_185249.pdf`). Processing a single specific file rather than scanning the folder is critical: without this, `ocr_rename.sh` would run in batch mode and process all pending PDFs on each invocation, consuming RAW files in the wrong order when text and graph jobs arrive within seconds of each other.

Reads the appropriate `.raw` file from `/home/pi/data/raw/` to identify the PDF. Since RetroPrinter generates its own filenames with no relation to the original `dfA0NNs12sun.raw` name, and since both RAW files of a QTG pair arrive on the RPi almost simultaneously, simple "most recent" selection would pick the wrong RAW. Instead, the script selects the RAW with the **lowest `dfA0NN` sequence number** among those available:

- The Sun always sends text before graph (dfA0NN then dfA0NN+1)
- RetroPrinter converts files in arrival order
- Therefore: 1st PDF converted (text) → takes the lowest sequence RAW; 2nd PDF (graph) → takes the remaining one

A fallback to most-recent is used if RAW files are not in the standard `dfA0NNs12sun.raw` format.

From the RAW content, the script extracts:

- **Simulator prefix** from the `SIMULATOR :` field → e.g. `[A330-200 RR]`
- **QTG test code** from the `CODE` pattern (primary), or top non-empty lines as fallback

Categorization rules applied in order:

1. If `SIMULATOR :` is present and a code is found → **text file** → `[SIM] CODE date §NNN.pdf`
2. If a code is found via top-lines fallback but contains **no digit** → rejected as graph noise (e.g. `PpGxaACCCArOxGgCcCppGP`) → `[GRAPH]`
3. If a code is found (CODE pattern or top lines) but **no `SIMULATOR :` field** → graph with readable header → `[GRAPH]`. SIERRA text files always contain `SIMULATOR :`; graph files do not, even when they carry the QTG code in their header.
4. If the RAW contains no extractable code after all filters → `[GRAPH]`
5. If no RAW is found at all → `[UNKNOWN]`

The spooler sequence number is extracted from the RAW filename and appended to the output PDF name as `§NNN` before the `.pdf` extension. This marker is what `group_qtg.sh` uses to pair files, and what the FTP step uses to hold back files not yet ready.

Example output: `[A330] B1 2.3 2026-06-21 14-32-01 §047.pdf`

### 4. Pairing and merging (`group_qtg.sh`)

Scans the PDF folder for files carrying a `§NNN` marker. Two files are considered a candidate pair if their sequence numbers are **consecutive modulo 1000** (handles the 999→000 rollover). However, not all consecutive pairs are valid — the following type rules are enforced:

| Combination | Action |
|-------------|--------|
| Text + `[GRAPH]` | ✅ Valid pair — text becomes page 1, graph page 2 |
| `[GRAPH]` + Text | ✅ Valid pair — order corrected regardless of arrival order |
| `[UNKNOWN]` + `[GRAPH]` | ✅ Allowed — RAW was missing at identification time, likely a text page; merged with a WARNING in the log |
| Text + Text | ❌ Rejected — two distinct QTG tests running consecutively; both go solo after timeout |
| `[GRAPH]` + `[GRAPH]` | ❌ Rejected — both go solo after timeout |
| `[UNKNOWN]` + Text | ❌ Rejected — too ambiguous; both go solo after timeout |
| `[UNKNOWN]` + `[UNKNOWN]` | ❌ Rejected — both go solo after timeout |

This prevents the most critical failure mode: a QTG that produces only a text file (no graph) being incorrectly merged with the text file of the following QTG test.

Files with no valid partner found are left in place until a **5-minute timeout** elapses, after which they are released as standalone PDFs (the `§NNN` marker is stripped). This handles QTGs that genuinely consist of a single file, and recovers from any failed or missing companion print job.

`group_qtg.sh` is also invoked by a cron job every 2 minutes, independently of `CustomScript.sh`, to ensure orphaned files are eventually released even if no further print jobs arrive in a session.

### 5. FTP transfer to MMGT (`CustomScript.sh`)

All PDFs in `/home/pi/data/pdf/` that do not already exist on the MMGT FTP server are transferred. Files still carrying a `§NNN` marker are explicitly skipped — they are waiting for their pair or timeout and must not be sent incomplete.

---

## File Reference

| File | Host | Role |
|------|------|------|
| `m.spts.sierra-deamon.sh` | Sun IPC | Polls spooler, transfers `.raw` files to RPi via FTP |
| `ocr_rename.sh` | RPi | Identifies and renames PDFs using RAW content; appends `§NNN` |
| `group_qtg.sh` | RPi | Pairs text+graph by sequence, merges to single PDF |
| `CustomScript.sh` | RPi | Orchestrator: called by RetroPrinter on each conversion |

---

## Naming Convention

| Stage | Example filename |
|-------|-----------------|
| Spooler (Sun) | `dfA047s12sun` |
| After `.raw` rename (Sun) | `dfA047s12sun.raw` |
| After RetroPrinter conversion (RPi) | `retro-printer_2026-06-21_143201-FULL.pdf` |
| After `ocr_rename.sh` (text) | `[A330] B1 2.3 2026-06-21 14-32-01 §047.pdf` |
| After `ocr_rename.sh` (graph) | `[GRAPH] GRAPH 2026-06-21 14-32-03 §048.pdf` |
| After `group_qtg.sh` (merged) | `[A330] B1 2.3 2026-06-21 14-32-01.pdf` |

---

## Deployment

### Prerequisites

- Raspberry Pi running RetroPrinter
- `poppler-utils` installed on the RPi:
  ```bash
  sudo apt install poppler-utils
  ```

### RPi setup

1. Copy `ocr_rename.sh`, `CustomScript.sh`, and `group_qtg.sh` to `/home/pi/`
2. Make executable:
   ```bash
   chmod +x /home/pi/ocr_rename.sh /home/pi/CustomScript.sh /home/pi/group_qtg.sh
   ```
3. Add the cron job for orphan timeout handling:
   ```bash
   crontab -e
   ```
   ```
   */2 * * * * /home/pi/group_qtg.sh >> /var/log/retroprinter.log 2>&1
   ```

### Sun IPC setup

1. Copy `m.spts.sierra-deamon.sh` to `/usr/u2/tools/scripts/`
2. Restart the daemon:
   ```sh
   kill <current_PID>
   /usr/u2/tools/scripts/m.spts.sierra-deamon.sh &
   ```

> **Deploy RPi first**, then the Sun daemon. This ensures the RPi is ready to handle back-to-back file arrivals before the 90s inter-file delay is removed from the Sun side.

---

## Logs

| Log file | Content |
|----------|---------|
| `/var/log/retroprinter.log` | `ocr_rename.sh` and `group_qtg.sh` activity |
| `/var/log/retroprinterTSK.log` | `CustomScript.sh` orchestration and FTP transfer activity |

Monitor in real time during a test session:
```bash
tail -f /var/log/retroprinter.log
tail -f /var/log/retroprinterTSK.log
```

---

## Key Design Decisions

**Sequence-based pairing over time-based pairing** — The Sun spooler assigns a monotonically incrementing 3-digit sequence number (`dfA0NN`) to each print job. Text and graph files from the same QTG test are always consecutive jobs (écart of 1, modulo 1000). Using this as the pairing key is more reliable than any time window, and removes the need for the 90-second inter-file delay that was previously required on the Sun side.

**Sequence-based RAW↔PDF matching** — RetroPrinter generates its own filenames (`retro-printer_YYYY-MM-DD_HHMMSS[-FULL|-N].pdf`) with no relation to the original `dfA0NNs12sun` spooler name, making name-based matching impossible. Both RAW files of a QTG pair arrive on the RPi almost simultaneously, so "most recent" selection picks the wrong one. Instead, `ocr_rename.sh` selects the RAW with the lowest `dfA0NN` sequence number: the Sun always sends text (dfA0NN) before graph (dfA0NN+1), RetroPrinter converts in arrival order, so the first PDF always corresponds to the lowest sequence RAW.

**`SIMULATOR :` field as the definitive text/graph discriminator** — Graph RAW files can contain readable text including the QTG code in their header, so extracting a code is not sufficient to classify a file as text. SIERRA text files always include a `SIMULATOR :` field; graph files never do. `ocr_rename.sh` requires both a QTG code and the `SIMULATOR :` field to classify a file as text. Any file with a code but no `SIMULATOR :` field is categorized as `[GRAPH]` regardless. An additional filter rejects top-lines candidates containing no digit, catching pure-letter graph noise patterns.

**Serialized processing via flock** — `CustomScript.sh` holds a blocking file lock for its entire duration. Concurrent invocations (one per converted file) queue up and execute sequentially, replacing the upstream 90-second delay with downstream serialization. `group_qtg.sh` uses a non-blocking lock when called from cron (skip and retry in 2 minutes if busy), but blocks when called from within `CustomScript.sh` via the parent lock.

**Non-destructive solo handling** — A file without a partner is never discarded or held indefinitely. After 5 minutes it is released as a standalone PDF, preserving normal non-QTG printing behaviour.
